import 'package:flutter/material.dart';

class AuthLoginTabletScreen extends StatelessWidget {
  const AuthLoginTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
