import 'package:flutter/material.dart';

class AuthLoginMobileScreen extends StatelessWidget {
  const AuthLoginMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
